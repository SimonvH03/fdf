Over The Horizon
	(utils_fdf.c) info in comments

Make
	(Makefile) write it myself

Cleanup
	(fdf_draw) make constant by extracting the redraw check and redraw = false set

Presets
	Take each preset into their own function (to apply when initialized)

Input
	add hotkey for squishing z values (map_iteration)