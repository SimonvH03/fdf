
(map _project) save previous Alpha, Beta, Gamma: only project if changed
(map_project) save original Map

(map project) offset and scale projections before drawing

(key input) make scale dynamic
	(map read) determine standard scale

(draw) plot lines between points
(draw) user interface

(map read) interpret colours
	(draw) plot line with colours

#(main) init perspective with defaults
(main) init fdf with defaults
