Map Init:
	- squish z values when total_height > x_max || y_max (maybe add toggle)

Presets:
	- if total map height is 0, and a globe is desired, make easter egg

Map read problems:
	- spaces at the end of a line
	- inconsistent line lengths

General safety:
	- Malloc fails: clear previous mallocs
