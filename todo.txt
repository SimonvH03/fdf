Colours
	(map read) interpret colours (or not, it kinda sucks)
	(colours) refine function for colouring earth

Menu
	(menu_draw) change menu text structure
	(fdf.h / fdf_init) change menu appearance

Make
	(Makefile) write it myself

Horizon
	(utils_fdf.c) pythagoras + move line point (or not, lotta work)

Map Iter
	make a single function to iterate the map, we need 1 double while loop at the very most
		map read
		fdf scale
		fdf project both
		map mods
		map colour
		...